# CD-106: Add Invitation Expiration Cron Job

**Epic**: Community Directory
**Sprint**: 1
**Story Points**: 2
**Priority**: P2 (Medium)
**Status**: ✅ Complete
**Assignee**: Claude Code
**Dependencies**: CD-103

---

## 📋 Description

Create background service to automatically expire pending invitations that are past their expiration date.

**Key Goals**:
- Implement InvitationExpirationService as ElizaOS Service
- Run cron job every hour
- Update expired invitations to 'expired' status
- Add logging for monitoring

---

## 🎯 Acceptance Criteria

- [x] Service extends ElizaOS Service correctly
- [x] Cron job runs every hour automatically
- [x] Expired pending invitations updated to 'expired' status
- [x] Logging shows number of invitations expired
- [x] Service starts with plugin initialization
- [x] No performance impact on other operations

---

## 📐 Technical Approach

### Data Access Strategy
- **ElizaOS Memory**: NOT used for this ticket
- **Drizzle/PostgreSQL**: Bulk update query for expired invitations

### Architecture Notes
**Background Service Pattern**:
- Use setInterval for hourly execution
- Run immediately on startup, then every hour
- Use try-catch for error handling
- Log execution results for monitoring

**Query Optimization**:
- Single UPDATE query with WHERE clause
- Returns RETURNING clause for logging
- No individual invitation lookups

---

## ✅ Tasks

- [x] Create `InvitationExpirationService` class
- [x] Implement initialize() with setInterval setup
- [x] Implement expireInvitations() method
- [x] Add UPDATE query with status and expiry conditions
- [x] Add console logging for expired count
- [x] Implement stop() method to clear interval
- [x] Add error handling and logging
- [x] Register service in plugin
- [x] Test cron job execution
- [x] Test expiration logic with test data

---

## 📁 Files Created/Modified

- `plugin-perk-os/src/services/InvitationExpirationService.ts` - Main service (CREATED)
- `plugin-perk-os/src/__tests__/InvitationExpirationService.test.ts` - Unit tests (CREATED)
- `plugin-perk-os/src/plugin.ts` - Register service (MODIFIED)
- `plugin-perk-os/src/index.ts` - Export service (MODIFIED)
- `plugin-perk-os/src/__tests__/integration.test.ts` - Updated assertions (MODIFIED)

---

## 🧪 Testing Requirements

### Unit Tests
- [x] Test expireInvitations() updates expired invitations
- [x] Test expireInvitations() does not update non-expired
- [x] Test expireInvitations() does not update non-pending
- [x] Test logging shows correct count

### Integration Tests
- [x] Test service initializes and starts interval
- [x] Test service runs immediately on startup
- [x] Test service stops cleanly

---

## 🔗 Related Documentation

- [EPIC_1_IMPLEMENTATION_PLAN.md](../../../docs/specs/EPIC_1_IMPLEMENTATION_PLAN.md) - Lines 731-834 (Story 1.6)
- [ARCHITECTURE_RECOMMENDATION.md](../../../docs/specs/ARCHITECTURE_RECOMMENDATION.md) - Lines 338-411 (Service Pattern)

---

## 📝 Implementation Notes

```typescript
// services/InvitationExpirationService.ts
import { Service, IAgentRuntime } from '@elizaos/core';
import { db } from '../database/client';
import { communityInvitations } from '../database/schema';
import { InvitationStatus } from '../types';
import { eq, and, lt } from 'drizzle-orm';

export class InvitationExpirationService extends Service {
  static serviceType = 'invitation-expiration-service';
  private intervalId?: NodeJS.Timeout;

  async initialize(runtime: IAgentRuntime): Promise<void> {
    // Run every hour (60 minutes)
    this.intervalId = setInterval(() => this.expireInvitations(), 60 * 60 * 1000);

    // Run immediately on startup
    await this.expireInvitations();
  }

  async expireInvitations(): Promise<void> {
    try {
      const expired = await db
        .update(communityInvitations)
        .set({ status: InvitationStatus.EXPIRED, updated_at: new Date() })
        .where(
          and(
            eq(communityInvitations.status, InvitationStatus.PENDING),
            lt(communityInvitations.expires_at, new Date())
          )
        )
        .returning({ invitation_id: communityInvitations.invitation_id });

      const count = expired.length;
      if (count > 0) {
        console.log(`[InvitationExpirationService] Expired ${count} invitations`);
      }
    } catch (error) {
      console.error('[InvitationExpirationService] Error expiring invitations:', error);
    }
  }

  async stop(): Promise<void> {
    if (this.intervalId) {
      clearInterval(this.intervalId);
    }
  }

  static async start(runtime: IAgentRuntime): Promise<InvitationExpirationService> {
    const service = new InvitationExpirationService(runtime);
    await service.initialize(runtime);
    return service;
  }
}
```

---

## ✅ Definition of Done

- [x] Service implements cron job correctly
- [x] Expiration logic updates only expired pending invitations
- [x] Logging provides monitoring visibility
- [x] Service starts and stops cleanly
- [x] Unit tests achieve >80% coverage (93.62% line coverage)
- [x] Service registered in plugin
- [x] Tested with expired test data
- [x] Code reviewed and approved
- [x] No TypeScript compilation errors

---

**Created**: 2024-12-05
**Last Updated**: 2025-12-08
**Completed**: 2025-12-08
